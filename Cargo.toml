[package]
name = "libsodium"
version = "0.1.1"
authors = ["Y. T. Chung <zonyitoo@gmail.com>"]
links = "sodium"
build = "build.rs"
license = "MIT/ISC"
repository = "https://github.com/zonyitoo/sodium-rs"
description = "Native bindings to the libsodium library"

[lib]
name = "libsodium"

[build-dependencies]
pkg-config = "0.3"

[dependencies]
libc = "0.2"
